# Email-Spam-Detection
This project builds a simple email spam detection system using Machine Learning. It uses CountVectorizer to convert text into features and a Multinomial Naive Bayes model to classify emails as spam or not, then evaluates accuracy and predicts new messages.
